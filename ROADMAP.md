# ROADMAP.md — the dependency ladder

The honest staged path from the current Mathlib floor up to mono-anabelian reconstruction. Each
rung is marked `NOT-STARTED` / `IN-PROGRESS` / `DONE` with its expected `DEBT` cost. **The bottom
rungs are concrete and near; the top rungs are genuinely multi-year and far.** The distance is not
compressed — saying so is the precondition for ever covering it.

Status as of **Pass 11 (2026-05-30)**. Inventory evidence for every "Mathlib has / lacks X" claim is
in `NOTES.md` (with real declaration names and file paths). Axiom classification convention — and the
anti-drift Reclassification rule — are in `AXIOM_LEDGER.md`.

A structural note on the ladder, because it governs the whole project:

> Rungs L1–L4 are **foundational inputs**: classical theory that is hard to formalize but *not the
> thing we are trying to prove*. They may legitimately be taken as `FOUNDATIONAL` axioms (honest
> boundary markers) or discharged as `DEBT` — either is allowed, because they sit **strictly below**
> the anabelian targets. Rungs R1–R3 (local reconstruction, Neukirch–Uchida, mono-anabelian) are the
> **targets**. Axiomatizing any of *those* — or anything that trivially implies them — is the
> cardinal sin (`CLAUDE.md`, rule 5) and is forbidden. The line between "input we may assume" and
> "theorem we must earn" runs between L4 and R1.

---

## L0 — Project floor + first seed   ·   **DONE** (Pass 0)   ·   DEBT: 0

- Fresh Lean 4 + Mathlib project pinned to `v4.30.0`, clean cached build.
- Mathlib inventory of all seven foundation areas (`NOTES.md`).
- One axiom-free, genuinely Galois-theoretic lemma: `Anabelian.fixingSubgroup_injective`
  (faithfulness of the infinite Galois correspondence) + its specialization to the absolute Galois
  group of a perfect field. Standard axioms only.
- Governance files (`AXIOM_LEDGER`, `ROADMAP`, `NOTES`) established.

This rung asserts nothing anabelian. It establishes that the *precondition* of reconstruction
(faithfulness: distinct subextensions ⟹ distinct subgroups) holds, and that the toolchain is clean.

---

## Foundational Mathlib gaps (L1–L4) — prerequisites, below the targets

### L1 — Galois theory of local/finite fields   ·   **IN-PROGRESS** (Passes 1–11)   ·   DEBT 1, FOUNDATIONAL 0

Mathlib **has** `IsNonarchimedeanLocalField` (definition, DVR, finite residue field, completeness),
abstract decomposition/inertia *subgroups* (`ValuationSubring.decompositionSubgroup`,
`inertiaSubgroup`) with the decomposition→residue-automorphism map, and — discovered Pass 1 — a rich
**finite-field** Galois API: `IsCyclic Gal(L/K)` for finite `L` (Frobenius generates),
`FiniteField.frobeniusAlgEquivOfAlgebraic`, `bijective_frobeniusAlgEquivOfAlgebraic_pow`,
`exists_forall_apply_eq_pow` (note: the Frobenius `orderOf … = finrank` lemmas are gated on
`[Finite L]`, so they do **not** apply to the infinite `AlgebraicClosure`).

**Done so far (axiom-free):**
- Pass 1 (`Anabelian/FiniteField.lean`): `absoluteGaloisGroup_mul_comm` — `Gal(𝔽_q̄/𝔽_q)` is
  commutative.
- Pass 2 (`Anabelian/FiniteField.lean`): `frobenius_zpowers_fixedField` and
  `frobenius_topologicalClosure_eq_top` — `Gal(𝔽_q̄/𝔽_q)` is **procyclic** (topologically generated by
  Frobenius). Procyclic ⟹ abelian, so Pass 2 subsumes and strengthens Pass 1.
- Pass 3 (`Anabelian/RationalsNonAbelian.lean`): `rationals_absoluteGaloisGroup_not_commutative` —
  `Gal(ℚ̄/ℚ)` is **non-commutative** (via `(X³-2).Gal ≅ S₃` pushed up). This **discharges owed
  witness W1**: `[Finite F]` is genuinely load-bearing for the Pass-1/2 lemmas.
- Pass 4 (`Anabelian/ResidueReduction.lean`): the **faithfulness half** of the residue reduction —
  `mem_inertiaSubgroup_iff` (inertia = pointwise residue stabilizer) and
  `residueReduction_quotient_injective` (`decomposition ⧸ inertia ↪ residue automorphisms`). Light
  (Mathlib's ramification API is definition-only), but genuine new ramification-territory content.
- Pass 5 (`Anabelian/UnramifiedQuotient.lean`): the **surjectivity half taken as `FOUNDATIONAL`**
  (option (B), the inflection point), then real structure built on it — `unramifiedQuotient_iso`
  (`G_K ⧸ N ≃* Gal(𝓀̄/𝓀)`) and `unramifiedQuotient_procyclic` (that quotient is procyclic, via Pass 2).
  **First non-empty ledger: `FOUNDATIONAL` +1** (`Anabelian.residueReduction_surjective`).
- Pass 6 (`Anabelian/FiniteFieldZHat.lean`): the **surjective half of `Gal(𝔽_q̄/𝔽_q) ≅ Ẑ`**, axiom-free
  — `zhatToGalois` (the canonical `Ẑ → Gal(K̄/K)` via the profinite-completion universal property) and
  `zhatToGalois_surjective` (closed range ⊇ dense Frobenius powers, via Pass 2). **No new boundary**;
  ledger stays `1 FOUNDATIONAL / 0 DEBT` (the anti-`FOUNDATIONAL`-stacking discipline).
- Pass 7 (`Anabelian/FiniteGaloisCyclic.lean`): the **per-level data of `≅ Ẑ`'s injective half**,
  axiom-free — `galoisFiniteField_mulEquivZMod`: `Gal(L/K) ≃* Multiplicative (ZMod (finrank K L))` for
  finite-field extensions (i.e. `Gal(𝔽_{q^n}/𝔽_q) ≅ ℤ/n`). A *complete* theorem (genuine but modest);
  closing the targeted whole `≅ Ẑ` was assessed not-axiom-free-this-pass, so this is the route-(i)
  fallback — **the iso was NOT posited**.
- Pass 8 (`Anabelian/ZHatProcyclic.lean`): the **`Ẑ`-side inverse-system presentation** of `≅ Ẑ`,
  axiom-free — `zhat_topologicalClosure_eq_top` (`Ẑ` is **procyclic**, the `Ẑ`-side analogue of Pass
  2) and `zhat_quotient_isCyclic` (every finite quotient of `Ẑ` is **cyclic**). The designated job was
  to *close* `≅ Ẑ`; inventory found closure blocked on the absent Galois-side level subfield `𝔽_{q^n}`
  and the iso was **NOT posited** — instead, real on-path injective-half machinery + a numbered
  Pass 9–11 sub-plan (below).
- Pass 9 (`Anabelian/FiniteFieldLevel.lean`): the **Galois-side level subfields** of `≅ Ẑ`, axiom-free
  (sub-plan rung Pass 9, **infrastructure**) — `levelField K n` = `𝔽_{q^n} ⊆ K̄` as a degree-`n`
  `FiniteGaloisIntermediateField`, the restriction `r_n` (`levelRestrict`, surjective), and the
  **Frobenius-aligned** generator (`levelRestrict_frobenius`: `r_n (Frob) = frobeniusAlgEquivOfAlgebraic`;
  `orderOf (r_n Frob) = n`). Graded as infrastructure (not the closed whole); the iso is **NOT** closed
  and **NOT** posited. Sets up Pass 10 (injectivity).
- Pass 10 (`Anabelian/FiniteFieldZHatIso.lean`): **CLOSES `Gal(𝔽_q̄/𝔽_q) ≅ Ẑ`**, axiom-free — the
  **first L1 whole of depth**. `zhatToGalois_injective` (`ker zhatToGalois = ⊥`, via
  `ker χ_m = closure⟨zhatGen^m⟩` + separation + Lagrange) and `galoisContinuousMulEquivZHat`
  (`Gal(𝔽_q̄/𝔽_q) ≃ₜ* Ẑ`, via continuous bijection of compacta). Nothing posited in the Pass 6–10 chain.
- Pass 11 (`Anabelian/SpectralValuation.lean`): **inflection decision = route (a)**, begin discharging
  the one boundary, axiom-free — `spectralIntegers K` (the spectral valuation ring `𝒪[K̄]`, a `Subring`)
  and `spectralIntegers_mem_iff_galois` (`Gal(K̄/K)` preserves `𝒪[K̄]`). The foundational strictly-lower
  brick (route step 1) of the residue-surjection discharge; on its strength,
  `residueReduction_surjective` was **reclassified `FOUNDATIONAL → DEBT`** (now `0 FOUNDATIONAL / 1
  DEBT`). Nothing cardinal-sin posited (the lifting is untouched).

**Sub-target IN-PROGRESS — the residue surjection (now `DEBT`, discharge begun Pass 11):**
- `Anabelian.residueReduction_surjective` (the surjectivity half `Gal(K̄/K) ↠ Gal(𝓀̄/𝓀)`; Pass 4 proved
  the *injective* half axiom-free) was `FOUNDATIONAL` (Passes 5–10) and is **reclassified
  `FOUNDATIONAL → DEBT` in Pass 11** (Reclassification log in `AXIOM_LEDGER.md`) — route (a), the
  principled discharge, is begun. **Cost: was FOUNDATIONAL 1; now DEBT 1.**
- **Pass-11 inflection decision (a) vs (b), driven by the common-prerequisite finding.** The valuation
  on `K̄` is the **common gate** for both (a) the residue-surjection discharge (it needs `𝓀[K̄]` + the
  reduction map) and (b) the ramification filtration (`G_i` are defined *via* the valuation, and the
  unramified/tame/wild picture sits *on* the residue reduction). So (b) is no independent escape from
  (a); (a) is the highest-leverage move. Chose (a).
- **Tractability correction to Pass 6.** "Valuation on `K̄` irreducibly absent" was **wrong**:
  `spectralNorm.normedField` + `NormedField.toValued` give `Valued K̄ ℝ≥0` (cf.
  `NumberTheory/Padics/Complex.lean` for `ℂ_p`), whence `𝒪[K̄]`/`𝓀[K̄]`; `Krasner.lean`'s `IsKrasner` is
  the lifting machinery. Only the final maximal-unramified lifting assembly is genuinely absent.
- **Discharge route (multi-pass; step 1 done, first step probe-verified):** (1) **valuation on `K̄`** —
  ✅ Pass 11 (`Anabelian/SpectralValuation.lean`): `spectralIntegers K = 𝒪[K̄]` (a `Subring`) +
  `spectralIntegers_mem_iff_galois` (`Gal(K̄/K)` preserves it), axiom-free; (2) **bridge**
  `IsNonarchimedeanLocalField K → NormedField K` (`ValuativeRel → Valued → RankOne → Valued.toNormedField`);
  (3) **residue field** `𝓀[K̄]` (via `NormedField.toValued`) + the reduction map; (4) **lifting /
  surjectivity** (`IsKrasner` + maximal-unramified) — the irreducible heart, **never posited** (that
  would be the cardinal sin). Discharging step 4 turns the `DEBT` into a theorem.

**Sub-target ✅ DONE (Pass 10) — `Gal(𝔽_q̄/𝔽_q) ≅ Ẑ`: the FIRST L1 WHOLE OF DEPTH, closed axiom-free.**
`Anabelian/FiniteFieldZHatIso.lean`: `galoisContinuousMulEquivZHat : galoisProfinite K ≃ₜ* ZHat` (the
full topological-group iso `Gal(𝔽_q̄/𝔽_q) ≃ₜ* Ẑ`) and `zhatToGalois_injective`. Standard axioms only;
**nothing posited** anywhere in the Pass 6–10 chain. The four assembled ingredients:
- **Surjective half** (Pass 6): `zhatToGalois_surjective`.
- **Per-level data** (Pass 7): `Gal(𝔽_{q^n}/𝔽_q) ≅ ℤ/n` (`galoisFiniteField_mulEquivZMod`).
- **`Ẑ`-side inverse-system** (Pass 8): `Ẑ` **procyclic** (`zhat_topologicalClosure_eq_top`), finite
  quotients **cyclic** (`zhat_quotient_isCyclic`), separation via `ProfiniteGrp.toLimit_injective`.
- **Galois-side level subfields** (Pass 9): `𝔽_{q^n} ⊆ K̄` degree `n` (`levelField`/`levelFGIF`), the
  **Frobenius-aligned** `r_n` (`levelRestrict`, surjective, `orderOf (r_n Frob) = n`).
- **Injectivity + packaging** (Pass 10): `ker χ_m = closure⟨zhatGen^m⟩` (`IsOpen.inter_closure` + Pass 8
  density; `ker χ_m` open via `krullTopology_discreteTopology_of_finiteDimensional`) ⟹
  `ker zhatToGalois = ⊥`; then `Continuous.homeoOfEquivCompactToT2` + `MulEquiv.ofBijective`. The
  "unique index-`n` subgroup" crux needed **no absent machinery** — `ker χ_m = closure⟨zhatGen^m⟩`
  replaced it.

**Inventory correction (Pass 8) to Pass 7's "absent" claim.** The general profinite-as-inverse-limit
machinery is **PRESENT**, not absent: `Mathlib.Topology.Algebra.Category.ProfiniteGrp.Limits` has
`toLimit`, `toLimit_injective` (separation via `exist_openNormalSubgroup_sub_open_nhds_of_one`),
`proj`, `isLimitCone`, `isoLimittoFiniteQuotientFunctor`, `continuousMulEquivLimittoFiniteQuotientFunctor :`
`P ≃ₜ* limit (diagram P)`. And `etaFn_injective_iff_residuallyFinite` concerns the **unit**
`η : G → completion G`, **not** `zhatToGalois` — so it does not deliver `zhatToGalois`-injectivity.

**The remaining obstruction, named precisely.** The full iso = injectivity of `zhatToGalois`, which
needs the commuting square `Ẑ → ℤ/n ≅ Gal(𝔽_{q^n}/𝔽_q) ← Gal(K̄/K)` at every level. The blocker is the
**Galois-side level projection** `Gal(K̄/K) → Gal(𝔽_{q^n}/𝔽_q)`: Mathlib has **no `𝔽_{q^n}` as a
`FiniteGaloisIntermediateField` of `AlgebraicClosure K`** (only standalone `GaloisField p n`,
`FieldTheory/Finite/GaloisField.lean`), so this restriction map is absent. Plus the *three* distinct
index systems must be matched cofinally: `FiniteIndexNormalSubgroup (Multiplicative ℤ)` (for the
`completion`-`Ẑ`), `OpenNormalSubgroup` (the generic `Limits` system), `(FiniteGaloisIntermediateField)ᵒᵖ`
(for `Gal`, via `mulEquivToLimit` / `asProfiniteGaloisGroupFunctor`).

**Numbered multi-pass sub-plan to close `≅ Ẑ` (axiom-free):**
- **Pass 9 — the Galois-side level subfield. ✅ DONE** (`Anabelian/FiniteFieldLevel.lean`).
  `levelField K n = fixedField (zpowers (Frob^n))` = `𝔽_{q^n} ⊆ K̄`, proved finite + Galois of **degree
  exactly `n`** (carrier = rootSet of the separable `X^(q^n)−X`, card `q^n`, `q^n = q^(finrank)`),
  bundled as `levelFGIF K n : FiniteGaloisIntermediateField`. Restriction
  `levelRestrict K n = r_n : Gal(K̄/K) →* Gal(𝔽_{q^n}/K)` (`restrictNormalHom`) with
  `levelRestrict_surjective`. **Frobenius alignment (the compatibility trap) handled:**
  `levelRestrict_frobenius` — `r_n (Frob) = frobeniusAlgEquivOfAlgebraic K (levelField K n)` — and
  `orderOf_levelRestrict_frobenius` — `orderOf (r_n Frob) = n`. So `r_n` sends the absolute Frobenius
  (`= zhatToGalois (η (ofAdd 1))`, Pass 6) to the Frobenius generator of `Gal(𝔽_{q^n}/K)`, **not** an
  arbitrary `zmodCyclicMulEquiv` generator. *Graded as infrastructure*, not the closed whole.
- **Pass 10 — injectivity + packaging. ✅ DONE** (`Anabelian/FiniteFieldZHatIso.lean`). `ker χ_m` is
  open (`χ_m` continuous to the discrete `Gal(𝔽_{q^m}/K)`), and `ker χ_m = closure⟨zhatGen^m⟩` via
  `IsOpen.inter_closure` + Pass 8 density (`χ_m (zhatGen^k) = 1 ↔ m ∣ k`). With separation + Lagrange
  this gives `ker zhatToGalois = ⊥` (`zhatToGalois_injective`); then
  `Continuous.homeoOfEquivCompactToT2` + `MulEquiv.ofBijective` package the iso
  (`galoisContinuousMulEquivZHat`). Packaging fell out the same pass, so **Pass 11 was absorbed**. The
  feared "procyclic ⟹ unique index-`n` subgroup" lemma was **not** needed — `ker χ_m = closure⟨zhatGen^m⟩`
  did the job directly. No `FOUNDATIONAL`, no `DEBT`; standard axioms only.

**Sub-targets, NOT-STARTED:**
- The unramified ⟶ tame ⟶ wild filtration of `Gal(K̄/K)` for local `K`.
- ~~Discharge owed witness W1 (`Gal(ℚ̄/ℚ)` non-abelian)~~ — **DONE, Pass 3**.
- Depends on: L0 + Mathlib's local-field/ramification/finite-field API.

**Honest read on L1 completeness (updated Pass 11).** The easy/finite fruit is harvested (P1–4);
Pass 5 took the one boundary; Passes 6–10 built and **closed** `≅ Ẑ` (the first L1 whole of depth,
axiom-free); Pass 11 made the **inflection decision to confront the boundary** rather than drift into
breadth. The project now holds **0 `FOUNDATIONAL`, 1 `DEBT`** — `residueReduction_surjective`,
reclassified because route (a) (its discharge) is **begun**: Pass 11 built the valuation-on-`K̄`
foundational brick (`spectralIntegers` = `𝒪[K̄]` + Galois-invariance) and **corrected** the Pass-6
"valuation absent" assessment (`spectralNorm.normedField`/`NormedField.toValued`/`IsKrasner` are
PRESENT). The valuation on `K̄` is the **common gate** for both the residue discharge and any
ramification-filtration work, so (a) is the highest-leverage path and (b) is no independent escape.
**Honest next step (Pass 12):** continue route (a) — the bridge `IsNonarchimedeanLocalField → NormedField`
(step 2) and/or the residue field `𝓀[K̄]` + reduction map (step 3), toward the lifting (step 4, the
`DEBT`'s heart). The metric is now net `DEBT` reduction: the one debt is committed and under
construction, no longer a static boundary. No second boundary; nothing cardinal-sin posited.

**Structural-hygiene debts (distinct from `DEBT` axioms and Owed witnesses — instance/setup cleanups
we owe before sustained work in a sub-area):**
- **D1 — the ℚ-algebra diamond.** Pass 3 resolved the `Algebra ℚ (AlgebraicClosure ℚ)` diamond
  (`DivisionRing.toRatAlgebra` vs `AlgebraicClosure.instAlgebra`) *locally*, per-theorem
  (`attribute [-instance] DivisionRing.toRatAlgebra in`). Per-theorem band-aids are fragile across
  Mathlib bumps. **Before sustained `AlgebraicClosure ℚ` / number-field work (R2-adjacent), resolve it
  once** — e.g. confirm `Subsingleton (Algebra ℚ (AlgebraicClosure ℚ))` or that the two instances
  agree definitionally, and provide a reusable lemma/instance — rather than dodging per-theorem.
  Trigger: the *second* recurrence of the diamond is when to attempt the structural fix instead of a
  second band-aid. Status: tracked, not yet triggered (Pass 3 = first appearance; Passes 4–5 did not
  recur — Passes 4–11 worked over abstract/local/finite fields, their algebraic closures, `Ẑ`/`Multiplicative ℤ`,
  and (Pass 11) an abstract nonarchimedean normed field — no `Algebra ℚ (AlgebraicClosure ℚ)`).

### L2 — Higher ramification groups (lower & upper numbering)   ·   **NOT-STARTED**   ·   DEBT: medium-high

**ABSENT** from Mathlib (re-confirmed Pass 11: no ramification filtration, no Herbrand function).
Needs: the filtration `G_i` in lower numbering, the Herbrand transition `ψ`/`φ`, upper numbering
`G^v`, and the Hasse–Arf theorem.
- **Common-prerequisite finding (Pass 11):** the lower-numbering `G_i` are defined *via the valuation
  on the extension*, and the unramified/tame/wild picture sits *on* the residue reduction (L1's
  `DEBT`). So L2 shares the **valuation-on-`K̄`** gate that route (a) (the residue-surjection
  discharge) is building (`Anabelian/SpectralValuation.lean`). L2 is therefore not independent of the
  L1 `DEBT` discharge — the same `𝒪[K̄]`/`𝓀[K̄]` infrastructure unblocks both.
- Depends on: L1, Mathlib valuation theory.
- Discharge: `DEBT` to be discharged, or `FOUNDATIONAL` if scoped out. Classical (Serre, *Local
  Fields*) — formalizable but a real body of work. Citation: J.-P. Serre, *Local Fields*, ch. IV.

### L3 — Local class field theory   ·   **NOT-STARTED**   ·   DEBT: high

**ABSENT** from Mathlib. The local reciprocity map `K^× → Gal^ab(K)`, the norm-residue isomorphism,
and the local existence theorem. Notably this *already exists outside Mathlib* in
`github.com/mariainesdff/LocalClassFieldTheory` (María Inés de Frutos-Fernández — the author of
Mathlib's `AbsoluteGaloisGroup.lean`), referenced from `Mathlib/RingTheory/Valuation/Discrete/Basic.lean`.
- Depends on: L1, L2, Galois cohomology (partial in Mathlib — to inventory).
- Discharge: strong candidate for `FOUNDATIONAL` (import/port the external development) **or**
  `DEBT`. Either is legitimate — it is below the targets. Citation: Serre, *Local Fields*, part 2.

### L4 — Global tools: Chebotarev, global reciprocity, Frobenius   ·   **NOT-STARTED**   ·   DEBT: high

Mathlib **has** number fields, adele rings (`NumberField.AdeleRing`), the Hermite–Minkowski
discriminant bound, and Frobenius in special cases (cyclotomic, finite fields). It **lacks**:
the **Chebotarev density theorem** (ABSENT — 0 hits), **global Artin reciprocity** / the idele-class
reciprocity map (ABSENT), and a general **arithmetic Frobenius at unramified primes** (only special
cases). These are exactly the inputs Neukirch's proof of R2 consumes.
- Depends on: L3, global CFT infrastructure, analytic number theory (for Chebotarev).
- Discharge: `FOUNDATIONAL` and/or `DEBT`; below the targets. Citation: Neukirch, *Algebraic Number
  Theory*, ch. V–VII; Neukirch–Schmidt–Wingberg, *Cohomology of Number Fields*.

---

## The targets (R1–R3) — must be EARNED, never axiomatized

### R1 — Local reconstruction (mono-anabelian, local)   ·   **NOT-STARTED**   ·   ⚠ multi-year

Recover the arithmetic of a local field `K` from its absolute Galois group `G_K` as a topological
group: residue characteristic `p`, residue cardinality `q`, ramification data, the multiplicative
monoid `O_K \ {0}` up to the relevant equivalence — group-theoretically and (the hard part)
*functorially*. This is the local heart of mono-anabelian geometry.
- Depends on: L1–L3.
- **First genuine anabelian theorems live here.** A realistic *first* target on this rung: recover
  `p` (or `q`) from a group-theoretic invariant of `G_K^ab` via L3. Citation: Mochizuki,
  *A version of the Grothendieck conjecture for p-adic local fields* (1997); Abrashkin.
- **Permitted `FOUNDATIONAL` inputs: L1, L2, L3.** These classical inputs sit below R1 and may be
  taken as boundary markers (logged + classified in `AXIOM_LEDGER.md`). No R-rung is ever a
  permitted `FOUNDATIONAL` input (targets must be earned — see the ledger Reclassification rule).
- `DEBT` budget: this rung's results are **forbidden as axioms**. They are the point.

### R2 — Neukirch–Uchida (number fields)   ·   **NOT-STARTED**   ·   ⚠⚠ major, multi-year on its own

A number field is determined up to isomorphism by its absolute Galois group: every topological
isomorphism `Gal(Q̄/K) ≅ Gal(Q̄/L)` is induced by a field isomorphism `K ≅ L`. The classical
predecessor of everything above. Proof skeleton: characterize decomposition groups group-
theoretically (they are the closed subgroups that are absolute Galois groups of local fields — needs
R1/L1), recover the prime data, then reconstruct via Chebotarev + global reciprocity (L4).
- Depends on: R1, L4 (Chebotarev, global reciprocity), the decomposition-group characterization.
- Citation: Neukirch (1969), Uchida (1976/77); Neukirch–Schmidt–Wingberg, *Cohomology of Number
  Fields*, ch. XII. Function-field analog: Uchida, Pop.
- **Permitted `FOUNDATIONAL` inputs: L1, L2, L3, L4** (notably Chebotarev and global reciprocity).
  **R1 must be a proved theorem here, never assumed** — assuming the local reconstruction R1 to get
  the global R2 would be reclassifying a target as foundational, which the ledger forbids.
- **Forbidden as an axiom.** This is the nearer-but-still-multi-year sub-target, *not* a starting
  point. It presupposes the entire stack L1–L4 + R1.

### R3 — Mono-anabelian reconstruction (the distant end goal)   ·   **NOT-STARTED**   ·   ⚠⚠⚠ distant

The functorial/algorithmic refinement above R2: a group-theoretic *algorithm* that inputs `G_K`
(plus monoid-theoretic data) and outputs the field functorially — "mono-anabelian transport." This
is the forcing construction the companion `iutt` project identified (by kernel-checked self-audit,
twice) as the only thing that converts IUT's "alien copies are non-identifiable" from a *stipulated*
into a *forced* property. The entire purpose of this repository is to earn this, rung by rung.
- Depends on: R2 and the full local mono-anabelian theory (R1 strengthened to functorial form).
- Citation: Mochizuki, *Topics in Absolute Anabelian Geometry* I–III; *Inter-universal Teichmüller
  Theory* I.
- **Permitted `FOUNDATIONAL` inputs: L1, L2, L3, L4. R1 and R2 must be proved theorems, never
  assumed.** Mono-anabelian recovery built on an *assumed* Neukirch–Uchida (R2) or local
  reconstruction (R1) would assert the project's own substance — forbidden.
- **The cardinal-sin axiom.** Stubbing this (or anything implying it) is the one move that would
  void the project. It is named here only to fix the direction of climb, not as anything near.

---

## One-line summary of the climb

```
L0 ✓ floor+seed → L1 local Galois → L2 higher ramification → L3 local CFT → L4 Chebotarev/global CFT
   → R1 local reconstruction → R2 Neukirch–Uchida → R3 mono-anabelian reconstruction
        \_________ inputs (may be FOUNDATIONAL) _________/   \________ targets (must be earned) ________/
```

Success = this ladder climbing while `AXIOM_LEDGER.md`'s `DEBT` count trends to zero. Not: a clean
`#print axioms` bought by relabeling R1/R2/R3 as `FOUNDATIONAL`.
